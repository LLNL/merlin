description:
    name: hello world workflow
    description: a very simple merlin workflow

batch:
    type: local

study:
    - name: step1 
      description: step 1
      run:
          cmd: |
              touch "$(GREET), world!"
              sleep 1

    - name: step2
      description: step 2
      run:
          cmd: |
              ls $(step1.workspace)
              sleep 1
          depends: [step1]

global.parameters:
    GREET:
        values : ["Hello","Bonjour"]
        label  : GREET.%%


