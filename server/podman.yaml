podman:
    command: podman
    init_command: \{command} .. (optional or default)
    start_command: \{command} run {image}
    stop_command: \{command} (optional or kill default)
    pull_command: \{command} pull {image} {url}
